# Central dongle (infused-kim fork uses ZMK_SPLIT_BLE_ROLE_CENTRAL, not ZMK_SPLIT_ROLE_CENTRAL;
# ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS is not in this fork — BT_MAX defaults for central come from app/Kconfig).
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_BLE_ROLE_CENTRAL=y
