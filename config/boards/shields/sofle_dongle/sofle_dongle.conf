# Dongle is central: two peripherals + usual BT profile slots (Kconfig must not redefine split symbols in user Kconfig.defconfig — parse order breaks them).
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2
CONFIG_BT_MAX_CONN=7
CONFIG_BT_MAX_PAIRED=7
